# Приклад змінних середовища. Реальні значення — тільки в .env (у .gitignore).
# Заповнюється на етапі "Environments & Delivery" (prompts/02-solution-setup.md).

APP_ENV=local
# DATABASE_URL=
# SECRET_KEY=
